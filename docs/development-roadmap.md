# Development Roadmap — SAA 2025 Web

## Phases

| # | Phase | Status | Notes |
|---|---|---|---|
| 0 | Bootstrap: Next.js + Supabase + test stack | ✅ Done (2026-08-28) | This commit |
| 1 | Spec discovery: pull specs/test cases from MoMorph, data model, clarifications | ✅ Done (2026-08-28) | 18 screens, 252 specs, 292 TCs → docs/momorph/, docs/data-model.md; 29 decisions in plans/clarifications.md (local). Commit b0a21a8 |
| 2 | Plan: /tkm:create-plan --e2e-test-first | ✅ Done (2026-08-28) | plans/260828-1257-saa-2025-web-login-homepage-awards/ (local): 4-feature spec draft, 8 phases, red-team 28 findings applied. Scope: Login, Homepage, Hệ thống giải + shell |
| 3 | Implementation: /tkm:takumi per phase | ✅ Done (2026-08-31) | 8/8 phases delivered (`plans/260828-1257-.../`). Group 1 (01–02): Supabase schema + auth predicate, design tokens + navigation shell. Group 2 (03): auth guard (`proxy.ts`) + OAuth callback, next-intl i18n, countdown logic, minimal 404/403, E2E session fixture. Group 3 (04–06, delivered 2026-08-28): Login/Homepage/Award System screen UI — `(site)`/`(auth)` route groups, `AWARD_CATEGORIES` trimmed to `{name, slug}`, inert-CTA a11y fix. Group 4a (07 + 07b, delivered 2026-08-30): session-aware header/FAB containers, sign-out Route Handler (replaces the Phase 03 Server Action — soft-navigation race), locale Server Action wrapper, `messages/en/*` catalogs + EN fallback rule, live countdown wrapper, per-route `generateMetadata`, remaining 13 body-copy components converted to `useTranslations`, `common.auth.loginError` key removed (no MoMorph source). Group 4b (08 + 08b, delivered 2026-08-31): full quality gate, a11y hardening (FAB `aria-expanded`/`aria-controls` on an always-mounted menu target, `common.fab.*` catalogue keys, countdown `aria-live="polite"`), `(auth)/layout.tsx` now wires `selectLocaleAction` (the `/login` language dropdown was a silent no-op), `e2e/support/with-retry.ts` bounded retry for Supabase auth timing, `docs/visual-qa/` + full `docs/test-traceability.md`, rebuild-spec Core promote (13 code-derived artifacts, user-approved). **Plan complete — 8/8 phases.** |
| 4 | Visual QA vs Figma + test traceability | ✅ Done (2026-08-31) | `docs/visual-qa/` — login, homepage, award-system, navigation-shell (+25 reference/capture images), each screen and each of the 5 shell states (guest/member/admin/dropdown-open/FAB-expanded) carries a MATCH/deviation verdict against a MoMorph reference frame. `docs/test-traceability.md` — full 94-case MoMorph traceability table (login 17, homepage 62, award system 15; dropdown ngôn ngữ has 0 published cases): 62 covered, 6 deferred, 26 not-covered, each with a cited test file+title or a stated reason. |
| 5 | Review, docs sync, PR | 🔄 In Progress (2026-08-31) | Group 4b inspection SEALED — score 8/10, 0 critical, 0 high, 2 Medium (both non-blocking): M1 (`docs/visual-qa/navigation-shell.md` stale claims) fixed same-day; M2 (roadmap, code-standards, project-changelog sync) closed. Round 1 (4 screens: Login, Homepage SAA, Hệ thống giải + shared shell) code-complete; PR: not yet opened — Round 2 began before it was. |
| 6 | Round 2 spec discovery + plan: Kudos cluster (Viết Kudo modal, `/kudos` live board, Add link, 3 dropdowns) | ✅ Done (2026-08-31) | `plans/260831-2303-saa-2025-web-kudos-round-2/` (local): F005_KudosCompose + F006_KudosLiveBoard spec drafts, round-2 data-model + architecture/permissions deltas, 8-phase blueprint (4 groups). Department seed (50 names) approved verbatim at the blueprint checkpoint. |
| 7 | Round 2 implementation: `/tkm:takumi` per phase | ✅ Done (2026-09-02) | Phases 01–07 delivered. Group 1 (01, alone): kudos schema — 8 tables, `kudos_card_view`, `create_kudos` RPC, storage policies, hashtag/department seed. Group 2 (02 ∥ 03 ∥ 04): read layer + derived stats, Viết Kudo compose modal UI, Kudos Live board UI — reviewed SEALED 9/10, 2 High fixes verified. Group 3 (05 ∥ 06): write layer (create/heart), detail + profile stub UI — reviewed SEALED 8/10, 2 High + 1 Med + 1 Low fixed & verified (storage insert-policy security fix, content depth/size caps, atomic heart-revoke, self-kudos block). Phase 07 (integration wiring: real submit, infinite scroll, heart/filters, EN catalogues) — green after test repairs, full e2e suite 89/89, gate (lint/typecheck/unit/build) all exit 0. |
| 8 | Round 2 hardening, visual QA, test traceability, docs promotion | ✅ Done (2026-09-02), final gate verified | `docs/test-traceability.md` — 123-case Kudos traceability table (Viết Kudo 57, Sun* Kudos Live board 41, Add link box 25): 96 covered, 1 deferred, 26 not-covered. The docs pass's own first draft flagged 5 not-implemented gaps (avatar/name → profile click ×4 TCs, no successful-Addlink-save test ×2 TCs); the avatar/name → profile click and the Addlink save both shipped within the round (verified on disk, re-counted programmatically). A same-day Group 4 review (`reports/reviewer-260902-1616-group4-integration-docs.md`) then caught and closed the other 2 gaps too — feed-card content click now navigates to `/kudos/[id]` and detail-page heart/copy-link are wired — re-verified SEALED (typecheck clean, lint 0 errors, targeted vitest 39/39, contractStatus OK); 1 genuine gap remains: a leaderboard entry's avatar/name click has zero test coverage and its hover-preview affordance doesn't exist in code at all (TC `6b1e2359`). `docs/features/F005_KudosCompose/`, `docs/features/F006_KudosLiveBoard/`, `docs/screens/SCR007_KudosCompose/`, `docs/screens/SCR008_KudosLiveBoard/` promoted from the Stage-1.5 spec drafts, reconciled to the shipped code. `docs/screens/SCR004_Fab/spec.md` corrected (Viết KUDOS now opens the compose modal). `docs/system-architecture.md`/`docs/data-model.md` gained the Kudos domain; `docs/system/permissions.md`'s profile-boundary statement replaced (profile-read is now the system's widest boundary, not its strictest). `docs/visual-qa/kudos-*.md` landed from a concurrent agent. Post-SEALED, two small product fixes landed and were re-verified: the header/footer "Sun* Kudos" link now points at `/kudos` (`e2e/navigation-shell.spec.ts` "06"), and `submitKudos` wires `validateImages` as its first gate (`docs/generated/api-map.md`'s dead-code finding closed); a fresh gen-gate re-baseline regenerated `docs/generated/*` + `docs/system/{overview,architecture,business-rules}.md`, and the affected SDD specs (F002, F005) were hand-reconciled. Final gate: `npm run typecheck` clean, `npm run lint` 0 errors, `submit-kudos.test.ts` 6/6 — full `test:e2e` re-run not repeated after these last two fixes (expensive; covered instead by reading the new/changed assertions directly, matching the Group 4 review's own practice). |

## Success metrics (DoD)

- UI matches Figma exactly (screenshot evidence in docs/visual-qa/).
- Logic matches MoMorph screen specs (traceability in docs/test-traceability.md).
- Unit + E2E tests written TDD-first; full gate green.

## Round 2 — Kudos cluster (2026-08-31 → 2026-09-02)

Adds F005_KudosCompose (Viết Kudo modal) and F006_KudosLiveBoard (`/kudos` board, `/kudos/[id]`
detail, `/profile` stub) on top of round 1's shell. Scope: compose + send a kudos (recipient,
TipTap-formatted message, 1–5 hashtags, up to 5 images, optional anonymity, optional link
insert), browse/filter/heart/search kudos on a live board, two decision-sourced minimal screens.
Deferred to a later round, in writing: Secret Box open flow, the real `/profile` screen,
mention/recipient notifications, an admin UI for `special_days`, and a real data source for the
gift-recipient leaderboard. See `docs/features/F005_KudosCompose/` and
`docs/features/F006_KudosLiveBoard/` for the full spec, and `docs/test-traceability.md` for
what's tested vs. not.
